import { Flex } from "antd";

const ChannelsContainer = () => {
  return (
    <Flex
      vertical
      style={{
        width: 300,
        border: "1px solid #f0f0f0",
        padding: 8,
      }}
    >
      Organization Name
    </Flex>
  );
};

export default ChannelsContainer;
